<h1>DASHBOARD</h1>

<% if current_user && current_user.admin? %>

<h1>Hello <%= current_user.email.split('@')[0].capitalize %></h1>

<%= link_to "Add Course" ,  new_course_path  %> ||
<%= link_to "Add students" ,  new_student_path  %>  ||
<%= link_to "Add lecturer" ,  new_lecturer_path  %> ||
<%= link_to "Add unit" ,  new_unit_path  %> ||
  <%= link_to "Signout" , destroy_user_session_path, 'data-turbo-method': :delete %>

<% else %>
  
<% end %>





